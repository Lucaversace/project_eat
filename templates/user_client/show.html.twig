{% extends 'base.html.twig' %}

{% block title %}Supprimer {{user_client.firstname}}{% endblock %}

{% block body %}
    <h1>{{user_client.firstname}}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user_client.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ user_client.email }}</td>
            </tr>
            <tr>
                <th>Firstname</th>
                <td>{{ user_client.firstname }}</td>
            </tr>
            <tr>
                <th>Lastname</th>
                <td>{{ user_client.lastname }}</td>
            </tr>
        </tbody>
    </table>

{#     <a href="{{ path('user_client_index') }}">back to list</a>

    <a href="{{ path('user_client_edit', {'id': user_client.id}) }}">edit</a> #}

    {{ include('user_client/_delete_form.html.twig') }}
{% endblock %}
