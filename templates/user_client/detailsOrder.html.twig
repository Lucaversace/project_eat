{% extends 'base.html.twig' %}

{% block title %}Details Commande {% endblock %}

{% block body %}

<section class="pricing-table">
   <div class="container">
         <div class="center">
             <h2 style="font-family: Roboto, sans-serif;">Commande au près du restaurant {{order.restaurant.restaurantName}}</h2>
             <p class="lead" style="font-family: Roboto, sans-serif;">Commande passé le {{order.date | date('d-m-Y à H:i')}}</p>
         </div>
        <div class="pricing-area text-center">
            <div class="row d-xl-flex justify-content-xl-center">
                <div class="col-sm-4 plan price red wow fadeInDown">
                    <ul class="list-group">
                        <li class="list-group-item heading" style="width: 373px;background: #009879;">
                            <h1 style="font-family: Roboto, sans-serif;">{{order.user.firstname}} {{order.user.lastname}}</h1>
                        </li>
                        {% for article in order.lineArticle %}
                            <li class="list-group-item"><span>{{article.quantity}} x {{article.dish.nom}} : {{article.price}} €</span></li>
                        {% endfor %}
                            <li class="list-group-item"><span>Etat : {{order.status}}</span></li>
                            <li class="list-group-item"><span><td>Total : {{order.price}} €</td></span></li>
                    </ul>
                 </div>
            </div>
        </div>
    </div>
</section>


{#     <div>
        <table>
            <tr>
                <td>Client</td>
                <td>{{order.user.firstname}} {{order.user.lastname}}</td>
            </tr>
            <tr>
                <td>Date de la commande</td>
                <td>{{order.date | date('d-m-Y à H:i')}}</td>
            </tr>
            {% for article in order.lineArticle %}
             <tr>
                <td>Produit {{loop.index}}</td>
                <td>{{article.dish.nom}}</td>
            </tr>
            {% endfor %}
              <tr>
                <td>Restaurant</td>
                <td>{{order.restaurant.restaurantName}}</td>
            </tr>
            <tr>
                <td>Statut de la commande</td>
                <td>{{order.status}}</td>
            </tr>
            <tr>
                <td>Total</td>
                <td>{{order.price}}</td>
            </tr>
        </table>
    </div> #}
{% endblock %}