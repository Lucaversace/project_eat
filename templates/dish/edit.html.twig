{% extends 'base.html.twig' %}

{% block title %}Modifier un plat{% endblock %}

{% block body %}
    <div class="text-center">
        <h4 style="color: #212529;font-size: 45px; margin-top:60px">Modifier mon plat</h4>
    </div>
    <hr class="modern-form__hr">

    {{ include('dish/_form.html.twig', {'button_label': 'Update'}) }}

{#     <a href="{{ path('dish_index') }}">back to list</a> #}

{#     {{ include('dish/_delete_form.html.twig') }} #}
{% endblock %}
