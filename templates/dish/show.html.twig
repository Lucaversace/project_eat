{% extends 'base.html.twig' %}

{% block title %}Supprimer {{ dish.nom }}{% endblock %}

{% block body %}
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ dish.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ dish.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ dish.description }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ dish.price }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td> <img src="{{ asset('uploads/' ~ dish.image) }}" style="height: 237px;width: 429px;" alt=" {{dish.nom}}"></td>
            </tr>
        </tbody>
    </table>
{# 
    <a href="{{ path('dish_index') }}">back to list</a> #}

 {#    <a href="{{ path('dish_edit', {'id': dish.id}) }}">edit</a> #}

    {{ include('dish/_delete_form.html.twig') }}
{% endblock %}
