<form method="post" class="form_delete" action="{{ path('restorer_delete', {'id': restorer.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer ce restaurant ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ restorer.id) }}">
    <button class="btn button_delete">Supprimer</button>
</form>
