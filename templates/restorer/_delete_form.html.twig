<form method="post" class="form_delete" {% if app.user.roles[0] == "ROLE_RESTORER" %}action="{{ path('restorer_delete', {'id': restorer.id}) }}" {% else %} action="{{ path('admin_delete_restorer', {'id': restorer.id}) }}" {% endif %} onsubmit="return confirm('Etes-vous sûr de vouloir supprimer ce restaurant ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ restorer.id) }}">
    <button class="btn button_delete">Supprimer</button>
</form>
