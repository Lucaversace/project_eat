{% extends 'base.html.twig' %}

{% block title %}Supprimer {{restorer.restaurantName}}{% endblock %}

{% block body %}
    <h1>{{restorer.restaurantName}}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ restorer.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ restorer.email }}</td>
            </tr>
            <tr>
                <th>RestaurantName</th>
                <td>{{ restorer.restaurantName }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ restorer.image }}</td>
            </tr>
        </tbody>
    </table>
{# 
    <a href="{{ path('restorer_index') }}">back to list</a>

    <a href="{{ path('restorer_edit', {'id': restorer.id}) }}">edit</a> #}

    {{ include('restorer/_delete_form.html.twig') }}
{% endblock %}
